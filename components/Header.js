import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Header = () => {
  return (
    <View>
      <Text style={{fontSize:24, textAlign:'right', padding:5, backgroundColor:'orange'}}>0</Text>
    </View>
  )
}

const styles = StyleSheet.create({

})

export default Header
